@using System.ComponentModel
@using Maynard.Minq.Blazor.Enums
@using Maynard.Minq.Blazor.Models
@using Microsoft.AspNetCore.Components
@using Microsoft.AspNetCore.Components.Web

@attribute [EditorBrowsable(EditorBrowsableState.Never)]

<div class="minq-settings-panel">
    @if (Parent.IsViewingSharedState)
    {
        <div class="minq-shared-alert">
            <span><strong>Shared View Active:</strong> Your local preferences are currently paused to display this URL.</span>
            <button class="minq-button minq-button-primary" style="margin: 0;" @onclick="Parent.SaveSharedAsDefault">Save as My Default</button>
        </div>
    }

    <MinqViewerSettingsGroup Title="Data">
        <MinqSlider Title="Page Size" Options="PageSizeOptions" @bind-Value="Parent.PageSize" OnChange="Parent.OnPageSizeChanged" Unit="records" />
        <div style="margin-top: 0.75rem;">
            <MinqSlider Title="Refresh Rate" Options="RefreshOptions" @bind-Value="Parent.RefreshInterval" OnChange="Parent.OnRefreshIntervalChanged" Unit="s" />
        </div>
    </MinqViewerSettingsGroup>

    <MinqViewerSettingsGroup Title="Appearance">
        <div style="display: flex; flex-direction: column; gap: 0.35rem;">
            <label style="font-size: 0.85rem; color: var(--minq-text-main); font-weight: 600;">Theme</label>
            <select class="minq-input" style="cursor: pointer;" value="@Parent.SelectedThemeName" @onchange="HandleThemeChange">
                @foreach (var theme in MinqViewer.AvailableThemes)
                {
                    <option value="@theme.Name">@theme.Name</option>
                }
            </select>
        </div>
        
        <div style="margin-top: 1rem;">
            <MinqSlider Title="Font Size" Options="FontSizeOptions" @bind-Value="Parent.TableFontSize" OnChange="Parent.SavePreferencesAsync" Unit="px" />
        </div>
    </MinqViewerSettingsGroup>

    <MinqViewerSettingsGroup Title="Columns">
        <div style="display: flex; flex-direction: column; gap: 0.25rem;">
            <MinqSlider Title="Pinned Column Width" Options="PinnedWidthOptions" @bind-Value="Parent.PinnedColumnWidth" OnChange="Parent.SavePreferencesAsync" Unit="px" />
            
            <div style="margin-top: 0.5rem;">
                <MinqSlider Title="Unpinned Column Width" Options="MaxWidthOptions" @bind-Value="Parent.MaxColumnWidth" OnChange="Parent.SavePreferencesAsync" Unit="px" />
            </div>
            
            <button class="minq-button" style="margin-top: 1rem;" @onclick="Parent.OpenColumnPicker">Choose Columns</button>
        </div>
    </MinqViewerSettingsGroup>

    <MinqViewerSettingsGroup Title="Table Options">
        <div style="display: flex; flex-direction: column; gap: 0.6rem; margin-top: 0.25rem;">
            <label class="minq-toggle-label">
                <div class="minq-switch">
                    <input type="checkbox" @bind="Parent.FlattenJsonProperties" @bind:after="Parent.OnViewDataChanged" />
                    <span class="minq-slider-round"></span>
                </div>
                Flatten Nested Objects
            </label>
            <label class="minq-toggle-label">
                <div class="minq-switch">
                    <input type="checkbox" @bind="Parent.HideDefaultValues" @bind:after="Parent.OnViewDataChanged" />
                    <span class="minq-slider-round"></span>
                </div>
                Hide Default Values
            </label>
        </div>

        <MinqRadio Title="Timestamp Format" 
                   Name="timestampFormat"
                   Options="TimestampOptions" 
                   @bind-Value="Parent.TimestampFormat" 
                   OnChange="Parent.SavePreferencesAsync" />
                   
        <MinqRadio Title="On Click" 
                   Name="rowClickBehavior"
                   Options="ClickOptions" 
                   @bind-Value="Parent.RowClickBehavior" 
                   OnChange="Parent.SavePreferencesAsync" />
    </MinqViewerSettingsGroup>

    <MinqViewerSettingsGroup Title="Danger Zone">
        <div style="margin-top: 0.5rem;">
            <button class="minq-button minq-button-danger" disabled="@(!Parent.CanDeleteCollection)" @onclick="Parent.OpenDeleteAllModal">Delete All Records</button>
        </div>
    </MinqViewerSettingsGroup>
</div>

@code {
    [CascadingParameter]
    public MinqViewer Parent { get; set; }
    
    private static readonly int[] PageSizeOptions = [10, 25, 50, 75, 100];
    private static readonly int[] RefreshOptions = Enumerable.Range(0, 31).Select(x => x * 10).ToArray();
    private static readonly int[] FontSizeOptions = Enumerable.Range(6, 21).ToArray();
    private static readonly int[] PinnedWidthOptions = Enumerable.Range(2, 9).Select(x => x * 50).ToArray();
    private static readonly int[] MaxWidthOptions = Enumerable.Range(2, 23).Select(x => x * 50).ToArray();

    // Add this explicit handler!
    private async Task HandleThemeChange(ChangeEventArgs e)
    {
        Parent.SelectedThemeName = e.Value?.ToString() ?? "Light Mode";
        await Parent.OnThemeChangedAsync();
    }

    private List<MinqRadioOption<TimestampFormatOption>> TimestampOptions => new()
    {
        new() { Value = TimestampFormatOption.Local, Label = "DateTime (Local)" },
        new() { Value = TimestampFormatOption.Utc, Label = "DateTime (UTC)" },
        new() { Value = TimestampFormatOption.Unix, Label = "Unix Timestamp (UTC)" },
        new() { Value = TimestampFormatOption.Elapsed, Label = "Elapsed Time" }
    };

    private List<MinqRadioOption<RowClickBehaviorOption>> ClickOptions => new()
    {
        new() { Value = RowClickBehaviorOption.SelectText, Label = "Select Text" },
        new() { Value = RowClickBehaviorOption.EditRecord, Label = "Edit Record" },
        new() { Value = RowClickBehaviorOption.DeleteRecord, Label = "Delete Record", IsDisabled = !Parent.CanDeleteSingle }
    };
}